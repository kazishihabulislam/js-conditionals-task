const name = "Alu";

const massage = 

name === "alu" 
// "Alu" is not equal to "alu" because of case sensitivity, so the condition will evaluate to false
? `Welcome Mr. ${name}` 
: `Who are you?`;

console.log(massage);
const studentName = "Kazi Shihabul Islam";

const studentMessage = 

studentName === "Kazi Shihabul Islam"
? `Welcome Mr. ${studentName}`
: `You are not allowed to enter`;

console.log(studentMessage);
